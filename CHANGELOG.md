# 更新记录

本文件记录每次公开更新的原因、行为变化、验证结果和兼容性影响。日期使用北京时间。

## 2026-09-08 — 横向三栏 Quick Reference 版面恢复

### 修改原因

`vis_qk_guide.pdf` 是横向三栏速查表。旧版只有单栏和双栏排序，中栏会被误判为跨栏内容，导致第一页按右栏、中栏、左栏穿插。第 2 页位于顶部的三个栏目标题还会被当作页眉删除；同一文字块内的正文、项目符号与命令也可能整块变成代码。

### 行为变化

- 增加横向三栏检测，按左栏、中栏、右栏依次输出，每栏内部保持从上到下，并继续保留跨栏页标题。
- 页眉删除缩小“无条件顶部区域”，更宽的顶部区域只删除已证实跨页重复的文字，因此保留贴近页顶的唯一栏目标题。
- 白字彩色横条标题会与下面的小节和正文分开。
- 同一原生文字块中的普通文字、项目符号、彩色标题和等宽命令按行分组；等宽判断改为按字符占比，单个 Courier 符号不再使整块正文变成代码。
- 密集的无框“命令/说明”双列清单恢复为 Markdown 表格，并合并 PyMuPDF 拆开的相邻续块。

### 验证结果

- 使用 3 页 `vis_qk_guide.pdf` 实测：三页均按三栏阅读顺序输出，第 2 页三个顶端栏目标题全部保留，第 3 页 `visualizer` 命令区生成 1 个 Markdown 表格。
- 23 项自动化回归测试全部通过，覆盖双栏兼容、三栏顺序、跨栏标题、顶部唯一标题、混合正文/代码拆分和无框命令表格。

### 兼容性影响

- 命令行参数、native/MinerU 自动分流和输出目录结构不变。
- 双栏论文继续使用原有左栏后右栏顺序；普通单栏页面不满足三栏持续内容条件时不会进入三栏排序。

## 2026-09-06 — 检测嵌套 Form XObject 中的复杂矢量图

### 修改原因

`Switch Level Time Simulation of CMOS Circuits with Adaptive Voltage and Frequency Scaling.pdf` 的第 5 页包含约 42.96 万个矢量对象，但绘图命令位于嵌套 Form XObject 中。页面自身内容流只有几百字节，上一版本的页面流阈值没有触发，导致本地表格和插图分析持续占用一个 CPU 核心。

### 行为变化

- 复杂度判定同时统计页面内容流和页面引用的嵌套 Form XObject 压缩流。
- 使用 `get_xobjects()` 枚举引用并读取压缩流大小，不预先展开矢量路径，因此普通页面的检查开销很小。
- 页面及嵌套绘图流合计超过 2 MiB 时，继续按整份 PDF 自动路由到 MinerU。
- 直接内容流与嵌套对象的 xref 会去重，避免重复统计。

### 验证结果

- 问题 PDF 现在约 1.4 秒识别出第 5 页含 2.9 MiB 复杂嵌套绘图内容，并在进入高开销本地分析前转入 MinerU。
- 增加“复杂度统计包含嵌套 Form XObject”的回归测试，共 23 项测试通过。

### 兼容性影响

- 命令行与输出结构不变。
- 之前可能在 native 路线卡住的嵌套矢量论文现在会使用 MinerU；显式 `--route native` 和 `--disable-mineru-ocr` 仍可禁止上传。

## 2026-09-06 — MinerU 上传进度与单层重试

### 修改原因

复杂矢量 PDF 已正确自动路由到 MinerU，但一个 14 MB 文件的状态在 `uploading` 保持数分钟。原因是上传函数内部最多执行 5 次 PUT，每次可等待 180 秒，外层任务还有 3 次重试；内部重试期间状态文件不会变化，因此看起来再次卡住。

### 行为变化

- 每个 MinerU 签名上传任务只执行一次 PUT。
- 上传失败立即交回外层任务，由外层把分片状态更新为 `retry`、申请新的签名地址并重试。
- 上传开始时显示文件名、大小和无响应超时。
- 每发送约 8 MiB 显示一次上传大小和百分比，成功后明确显示上传完成。
- 保留外层 3 次任务重试，避免减少网络故障恢复能力，同时消除 5×3 的嵌套重试放大。

### 验证结果

- 增加“即使 API 重试次数为 5，每个签名上传任务也只执行一次 PUT”的回归测试。
- 自动分流、防卡、下载续传和文档结构测试继续通过，共 22 项。

### 兼容性影响

- 命令行、输出路径、MinerU 模型及 OCR 参数不变。
- 网络失败会更早刷新任务状态并取得新上传地址；旧 `.mineru_cache` 状态可继续使用。

## 2026-09-06 — 超复杂矢量页面自动转 MinerU

### 修改原因

上一版本为了防止数十万个矢量路径导致本地解析卡住，会跳过复杂页的矢量插图和矢量表格分析。用户要求这类 PDF 不再生成省略矢量结构的 native 结果，而是自动交给 MinerU 完整处理。

### 行为变化

- 复杂矢量判定移到 PDF 自动分流阶段。
- 任意页面的压缩绘图内容超过 2 MiB 时，默认 `--route auto` 将整份 PDF 路由到 MinerU OCR。
- 分流仍以整份 PDF 为单位，不混合 native 和 MinerU 结果。
- 显式 `--route native` 保持禁止上传的语义：遇到超复杂矢量页时停止并显示页码、大小和原因。
- 页面渲染阶段的复杂度保护继续保留，作为绕过自动分流调用底层函数时的防卡兜底。

### 验证结果

- 增加“超复杂矢量页将整份 PDF 自动路由到 MinerU”的回归测试。
- 原有防卡、扫描件分流、MinerU 续传及结构转换测试继续通过，共 21 项。

### 兼容性影响

- 命令行参数和输出结构不变。
- 以前会由 native 路线跳过矢量分析的 PDF，现在默认会上传整份文档到 MinerU；敏感文档可使用 `--route native` 或 `--disable-mineru-ocr` 禁止上传。

## 2026-09-06 — 防止超复杂矢量页面卡住

### 修改原因

`Cross-PUF Attacks on Arbiter-PUFs through their Power Side-Channel.pdf` 的第 3 页包含约 47.7 万个矢量绘图对象。PyMuPDF 在矢量插图识别和表格识别阶段会展开全部路径，导致目录批处理长时间停留在该 PDF 且没有进度输出。

### 行为变化

- 在调用高开销的矢量插图和矢量表格分析之前，先检查页面压缩内容流大小。
- 单页压缩内容流超过 2 MiB 时，跳过该页的矢量插图和矢量表格分析，避免展开数十万个绘图路径。
- 跳过范围只限于绘图分析；原生正文、标题和嵌入图片继续正常提取。
- 终端会显示具体页码、内容流大小以及跳过的分析类型，避免再次出现无提示等待。

### 验证结果

- 问题 PDF 的 5 页转换由长时间无响应缩短到约 7.4 秒，生成 5 个页面标记和 7 个图片引用。
- 增加“超复杂矢量页跳过绘图分析但保留正文”的回归测试，共 20 项自动化测试通过。

### 兼容性影响

- 命令行参数、自动分流和输出目录结构不变。
- 极端复杂页面不会尝试恢复纯矢量插图或矢量线表格；页面已有的嵌入图片和文字仍会输出。普通页面继续使用原有的插图与表格识别流程。

## 2026-09-05 — MinerU 结果下载断点续传

### 修改原因

MinerU 已完成解析，但 CDN 下载约 30 MB 的结果 ZIP 时多次中途断开。旧版本每次重试都会删除已下载内容并从零开始，而且连续无数据超时为一小时，终端长时间没有进度时容易被误认为程序卡死。

### 行为变化

- 结果 ZIP 使用 `.part` 文件保存未完成数据。
- CDN 支持 HTTP Range 时从已有字节继续下载；旧版本留下的不完整 `.zip` 会自动迁移为续传文件。
- Windows 上通过复制迁移旧缓存，最终 ZIP 校验成功前保留原文件；若另一个进程仍占用缓存，会明确提示先停止旧进程。
- 系统网络、公开 IP 直连和不同公开 IP 之间共享同一份续传数据。
- 服务器忽略 Range 时改用独立的从头下载文件；失败后只保留较长的数据，续传进度不会回退。
- 每新增约 8 MiB 显示一次已下载大小和百分比。
- 下载连续无数据的默认超时由 3600 秒调整为 180 秒；只要持续收到数据就不会超时。
- 文件上传连续无响应的默认超时也由 3600 秒调整为 180 秒，避免小分片长时间停在 `uploading`。
- 恢复旧任务时立即把文档状态从 `failed` 更新为 `processing`，并清除过期错误信息。
- 完成后核对总字节数和 ZIP 结构，确认完整才进入解压阶段。

### 验证结果

- 增加“从旧版半截 ZIP 通过 Range 续传并校验”的回归测试。
- 增加“不支持 Range 的失败下载不会覆盖较长缓存”测试，共 19 项自动化测试通过。

### 兼容性影响

- PDF 上传、MinerU 任务及已经完成的解析结果不受影响。
- 当前 `[Front cover].pdf` 的第 1 分片可从保留的约 25 MiB 继续下载，第 2 分片仍按原流程处理。

## 2026-09-05 — 修复 MinerU 完成后找不到 Markdown

### 修改原因

包含空格等字符的 PDF 名称经过主程序和 MinerU 两套不同的安全文件名规则处理。MinerU 已成功生成 Markdown 和图片，但主程序只按下划线形式查找文件，因而误报“没有生成 Markdown 文件”。

### 行为变化

- 优先读取 MinerU 元数据记录的 Markdown 文件名。
- 元数据已被旧版本清理时，会在结果根目录查找唯一的 Markdown 文件。
- 将找到的文件统一重命名为最终输出目录使用的规范名称。
- 找不到文件或存在多个候选文件时，错误信息会包含结果目录或候选文件名，便于诊断。
- 保留旧任务缓存；重新运行时可以直接整理已经下载的结果，无需再次上传 PDF。

### 验证结果

- 使用带方括号和空格的 MinerU 结果文件名增加回归测试。
- 共 17 项自动化测试通过。

### 兼容性影响

- 命令、输出目录结构及 MinerU API 参数不变。
- 旧版留下的成功缓存可直接恢复。

## 2026-09-04 — PDF 级自动 OCR 分流与 `.env` 配置

### 修改原因

目录批处理和单文件处理需要采用一致、明确的自动分流规则，并避免在同一份 Markdown 中混合两种转换引擎。

### 行为变化

- `--route auto` 继续作为单文件和目录批处理的默认值。
- 目录批处理会对每个 PDF 独立判断使用本地原生解析还是 MinerU OCR。
- 分流颗粒度固定为整份 PDF，不增加页面级混合路线。
- 如果任意页面被判定为整页扫描图，整份 PDF 将交给 MinerU OCR。
- 只有隐藏 OCR 文字层或整份文档原生可见文字不足时，也会整份使用 MinerU OCR。
- 启动器现在会自动读取程序目录、程序上一级、当前目录或输入目录中的 `.env`，支持 `MINERU_TOKEN` 和 `MINERU_API_TOKEN`。
- `.env` 支持单引号、双引号、`export` 前缀和行尾注释；Token 不会写入日志。
- OCR 未配置提示不再出现连续两个句号。

### 验证结果

- 增加“原生文字页 + 扫描页 + 原生文字页”的混合 PDF 回归测试。
- 增加 `.env` Token 加载回归测试，共 16 项自动化测试通过。

### 兼容性影响

- 命令行参数和输出目录结构没有变化。
- 已有的系统环境变量仍优先于 `.env`；原有 `mineru_token.txt` 和 `--mineru-token-file` 继续有效。
- 混合 PDF 可能从原生解析改为整份 MinerU OCR，因此会上传整份 PDF；可用 `--route native` 或 `--disable-mineru-ocr` 禁止上传。

## 2026-09-04 — 特殊图片与页眉页脚修复

### 行为变化

- 规范化 Indexed、ICCBased 等特殊 PDF 图片的色彩空间。
- soft mask 与彩图尺寸不一致时自动缩放；仍无法解码时使用页面区域渲染兜底。
- 自动去除跨页重复的页眉、页脚和页码。
- 目录批处理再次运行时默认跳过已有结果，只重试失败或尚未完成的文件。

### 验证结果

- 检查 995 张 PDF 图片，未出现 PNG 写入失败。
- 完整转换一份 252 页手册，重复页眉和页脚未进入 Markdown。

## 2026-09-04 — 首次公开版本

### 行为变化

- 提供自包含的 PDF/DOCX 转 Markdown 程序、虚拟环境安装器和跨平台启动器。
- 支持原生 PDF 的标题、正文、列表、代码、表格和图片提取。
- 支持 MinerU OCR 自动分流、大文件分片、断点续传和结果合并。
- 支持 DOCX 标题、列表、表格、图片和代码段转换。
- 固定单文件与目录批处理的输出结构。

### 安全与内容

- 未上传任何样例 PDF、DOCX、转换结果、Token 或运行缓存。
