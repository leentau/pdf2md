# 更新记录

本文件记录每次公开更新的原因、行为变化、验证结果和兼容性影响。日期使用北京时间。

## 2026-09-05 — 修复 MinerU 完成后找不到 Markdown

### 修改原因

包含空格等字符的 PDF 名称经过主程序和 MinerU 两套不同的安全文件名规则处理。MinerU 已成功生成 Markdown 和图片，但主程序只按下划线形式查找文件，因而误报“没有生成 Markdown 文件”。

### 行为变化

- 优先读取 MinerU 元数据记录的 Markdown 文件名。
- 元数据已被旧版本清理时，会在结果根目录查找唯一的 Markdown 文件。
- 将找到的文件统一重命名为最终输出目录使用的规范名称。
- 找不到文件或存在多个候选文件时，错误信息会包含结果目录或候选文件名，便于诊断。
- 保留旧任务缓存；重新运行时可以直接整理已经下载的结果，无需再次上传 PDF。

### 验证结果

- 使用带方括号和空格的 MinerU 结果文件名增加回归测试。
- 共 17 项自动化测试通过。

### 兼容性影响

- 命令、输出目录结构及 MinerU API 参数不变。
- 旧版留下的成功缓存可直接恢复。

## 2026-09-04 — PDF 级自动 OCR 分流与 `.env` 配置

### 修改原因

目录批处理和单文件处理需要采用一致、明确的自动分流规则，并避免在同一份 Markdown 中混合两种转换引擎。

### 行为变化

- `--route auto` 继续作为单文件和目录批处理的默认值。
- 目录批处理会对每个 PDF 独立判断使用本地原生解析还是 MinerU OCR。
- 分流颗粒度固定为整份 PDF，不增加页面级混合路线。
- 如果任意页面被判定为整页扫描图，整份 PDF 将交给 MinerU OCR。
- 只有隐藏 OCR 文字层或整份文档原生可见文字不足时，也会整份使用 MinerU OCR。
- 启动器现在会自动读取程序目录、程序上一级、当前目录或输入目录中的 `.env`，支持 `MINERU_TOKEN` 和 `MINERU_API_TOKEN`。
- `.env` 支持单引号、双引号、`export` 前缀和行尾注释；Token 不会写入日志。
- OCR 未配置提示不再出现连续两个句号。

### 验证结果

- 增加“原生文字页 + 扫描页 + 原生文字页”的混合 PDF 回归测试。
- 增加 `.env` Token 加载回归测试，共 16 项自动化测试通过。

### 兼容性影响

- 命令行参数和输出目录结构没有变化。
- 已有的系统环境变量仍优先于 `.env`；原有 `mineru_token.txt` 和 `--mineru-token-file` 继续有效。
- 混合 PDF 可能从原生解析改为整份 MinerU OCR，因此会上传整份 PDF；可用 `--route native` 或 `--disable-mineru-ocr` 禁止上传。

## 2026-09-04 — 特殊图片与页眉页脚修复

### 行为变化

- 规范化 Indexed、ICCBased 等特殊 PDF 图片的色彩空间。
- soft mask 与彩图尺寸不一致时自动缩放；仍无法解码时使用页面区域渲染兜底。
- 自动去除跨页重复的页眉、页脚和页码。
- 目录批处理再次运行时默认跳过已有结果，只重试失败或尚未完成的文件。

### 验证结果

- 检查 995 张 PDF 图片，未出现 PNG 写入失败。
- 完整转换一份 252 页手册，重复页眉和页脚未进入 Markdown。

## 2026-09-04 — 首次公开版本

### 行为变化

- 提供自包含的 PDF/DOCX 转 Markdown 程序、虚拟环境安装器和跨平台启动器。
- 支持原生 PDF 的标题、正文、列表、代码、表格和图片提取。
- 支持 MinerU OCR 自动分流、大文件分片、断点续传和结果合并。
- 支持 DOCX 标题、列表、表格、图片和代码段转换。
- 固定单文件与目录批处理的输出结构。

### 安全与内容

- 未上传任何样例 PDF、DOCX、转换结果、Token 或运行缓存。
